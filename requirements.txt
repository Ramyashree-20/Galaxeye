# ==================================================================
# Direct dependencies for galaxeye_change_detection.
# Transitive packages are resolved automatically by pip.
#
# NOTE: torch / torchvision are intentionally NOT pinned here.
# Install them with the CUDA/CPU build that matches your environment
# (see https://pytorch.org/get-started/locally/). For example:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then:
#   pip install -r requirements.txt
# ==================================================================

# Core deep-learning utilities
torch>=2.0
segmentation-models-pytorch==0.3.3

# Data + augmentation
albumentations==1.3.1
opencv-python==4.8.1.78
numpy==1.24.3

# Metrics
scikit-learn==1.3.0

# Config + logging
pyyaml==6.0
tqdm==4.65.0

# Visualization
matplotlib==3.7.1

# Notebooks (optional, used by notebooks/data_exploration.ipynb)
jupyterlab==4.0.1
ipykernel==6.29.5
